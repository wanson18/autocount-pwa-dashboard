# AutoCount Cloud API Credentials (server-side only)
AUTOCOUNT_API_URL=https://accounting-api.autocountcloud.com

# Legacy Sales variables, also used by the optional shared dispatch fallback.
# Keep this one API key only when intentionally using that fallback; preferred
# dispatch configuration uses the company-specific credential pairs below.
AUTOCOUNT_API_KEY=your-api-key-here
AUTOCOUNT_KEY_ID=your-key-id-here
AUTOCOUNT_ACCOUNT_BOOK_ID=your-sales-account-book

# Required account books. Keep these values distinct and never accept them from a client.
AUTOCOUNT_ACCOUNT_BOOK_WANSON_ENTERPRISE=your-enterprise-account-book
AUTOCOUNT_ACCOUNT_BOOK_WANSON_SDN_BHD=your-sdn-bhd-account-book

# Prefer a separate Key-ID/API-Key pair for each account book.
AUTOCOUNT_KEY_ID_WANSON_ENTERPRISE=your-enterprise-key-id
AUTOCOUNT_API_KEY_WANSON_ENTERPRISE=your-enterprise-api-key
AUTOCOUNT_KEY_ID_WANSON_SDN_BHD=your-sdn-bhd-key-id
AUTOCOUNT_API_KEY_WANSON_SDN_BHD=your-sdn-bhd-api-key

# Sales Dashboard aliases for the same two fixed books. These are accepted
# alongside the dispatch names above when the dashboard loads both books.
AUTOCOUNT_ENTERPRISE_KEY_ID=your-enterprise-key-id
AUTOCOUNT_ENTERPRISE_API_KEY=your-enterprise-api-key
AUTOCOUNT_SDN_BHD_KEY_ID=your-sdn-bhd-key-id
AUTOCOUNT_SDN_BHD_API_KEY=your-sdn-bhd-api-key

# Optional shared dispatch fallback. Set AUTOCOUNT_API_KEY_ID together with
# the single AUTOCOUNT_API_KEY above, or leave the shared fallback unset.
AUTOCOUNT_API_KEY_ID=your-shared-key-id

# Cache TTL in minutes (default: 10)
CACHE_TTL_MINUTES=10

# Set to "true" to use mock data instead of live API
USE_MOCK_DATA=false

# IANA timezone used to decide what "today" means for default date ranges.
# Serverless hosts run in UTC, so this must be set explicitly (default: Asia/Kuala_Lumpur)
REPORT_TIMEZONE=Asia/Kuala_Lumpur

# Pooled Postgres connection for dispatch persistence. Use a pooled provider URL.
DATABASE_URL=postgres://user:password@host:5432/database?sslmode=require

# Dispatch clerk authentication (server-side only; leave blank until configured).
# DISPATCH_SESSION_SECRET must be base64url for exactly 32 random bytes (43 chars,
# no '=' padding). Generate one with a cryptographically secure random generator.
# DISPATCH_USERS_JSON must be a JSON array of objects with exactly clerkId, role,
# active, and a canonical scrypt$N$r$p$salt$derived-key pinHash field. The only
# approved profile is N=16384, r=8, p=1, a 16-byte salt, and a 64-byte key.
# Alternate bounded profiles, malformed encodings, and duplicate parameters fail closed.
DISPATCH_USERS_JSON=
DISPATCH_SESSION_SECRET=
# Set only when the deployment's forwarded client address is trusted. Leave blank
# on platforms where the runtime already supplies a trusted request IP.
DISPATCH_TRUSTED_PROXY=

# Optional temporary Postgres URL for the repository integration test run.
# Without this value, the suite uses in-memory WASM PostgreSQL.
# TEST_DATABASE_URL=postgres://user:password@host:5432/test_database?sslmode=require
